Exercise:4 windows problem solving

issus with the web application installed when browsing to http://localhost we get 503 exception

As check in IIS configuration,Application pool is not correctly configure so we get 503 error.

Application pool --->change DefaultAppPool to .Net v2.0

After change the Application pool site working fine
